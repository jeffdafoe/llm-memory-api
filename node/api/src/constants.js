// Shared constants used across the application.

// Session kinds — used in the sessions table 'kind' column.
const SESSION_KIND = {
    API: 'api',
    WEB: 'web'
};

module.exports = { SESSION_KIND };
