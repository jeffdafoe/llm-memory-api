all:
  hosts:
    vps:
      ansible_host: 165.245.142.212
      ansible_user: jeff
      ansible_python_interpreter: /usr/bin/python3
  vars:
    deploy_environment: production
    server_name: memory.jeffdafoe.com
    app_user: jeff
    database_name: memory_api
    database_user: memory_api
    database_password: "{{ lookup('env', 'MEMORY_DB_PASSWORD') }}"
    memory_api_key: "{{ lookup('env', 'MEMORY_API_KEY') }}"
    openai_api_key: "{{ lookup('env', 'OPENAI_API_KEY') }}"
